spring:
  application:
    name: handdoc
  config:
    import: optional:file:.env[.properties]
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

naver:
  clova:
    api-key-id: ${CLOVA_API_KEY_ID}
    api-key: ${CLOVA_API_KEY}
